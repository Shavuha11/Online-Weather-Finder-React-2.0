function StatusCard ({title, text}) {
    return (
        <div className="status-card">
            <h2>{title}</h2>
            <p>{text}</p>
        </div>
    )
}

export default StatusCard;
